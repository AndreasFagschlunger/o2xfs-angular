import { DeviceStateDirective } from './device-state.directive';

describe('DeviceStateDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceStateDirective();
    expect(directive).toBeTruthy();
  });
});
